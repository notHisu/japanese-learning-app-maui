<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="JapaneseLearningApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:JapaneseLearningApp"
    xmlns:views="clr-namespace:JapaneseLearningApp.Views"
    Title="JapaneseLearningApp">

    <ShellContent Title="Hiragana" ContentTemplate="{DataTemplate views:Hiragana.HiraganaMenuPage}" Route="HiraganaMenuPage"/>

    <ShellContent Title="Browse" ContentTemplate="{DataTemplate views:Hiragana.HiraganaBrowsePage}" Route="hiragana/browse"/>
    <ShellContent Title="Practice" ContentTemplate="{DataTemplate views:Hiragana.HiraganaPracticePage}" Route="hiragana/practice"/>
    <ShellContent Title="Quiz" ContentTemplate="{DataTemplate views:Hiragana.HiraganaQuizPage}" Route="hiragana/quiz"/>

    <!--<ShellContent Title="Home" ContentTemplate="{DataTemplate views:HomePage}"/>
    <ShellContent Title="Vocabulary" ContentTemplate="{DataTemplate views:VocabularyPage}" Route="VocabularyPage"/>
    <ShellContent Title="Kanji" ContentTemplate="{DataTemplate views:KanjiPage}" Route="KanjiPage"/>
    <ShellContent Title="Quiz" ContentTemplate="{DataTemplate views:QuizPage}" Route="QuizPage"/>-->

</Shell>
